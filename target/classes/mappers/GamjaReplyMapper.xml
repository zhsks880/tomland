<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.spring.TomLand.gamjareply.mapper.IGamjaReplyMapper">

	<!-- 답변 등록 -->
	<insert id="qnaRegist">
		insert into gamjaboardreply(rno, qna_writer, qna_reply, qna_bno, gno)
		values(gamjaboardreply_seq.nextval, #{qnaSeller}, #{qnaReply}, #{qnaReplyBno}, #{qnaReplyGno})
	</insert>



</mapper>